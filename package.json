{
  "name": "dadosagora",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "eleventy --config=.eleventy.cjs --input=src --output=_site",
    "start": "eleventy --serve --config=.eleventy.cjs --input=src --output=_site"
  },
  "dependencies": {
    "@11ty/eleventy": "^3.0.1",
    "@quasibit/eleventy-plugin-sitemap": "^2.2.0"
  }
}
