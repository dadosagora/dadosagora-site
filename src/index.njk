---
title: Conversor de Moedas Online
description: Converta moedas em tempo real (dólar, euro, bitcoin, peso, libra e mais) com cotações atualizadas minuto a minuto. Ferramentas de conversão de temperatura, distância, peso, volume e tempo — tudo grátis, online e em português.
layout: layout.njk
---

<h2>Cotações agora</h2>
<p>Converta moedas em tempo real: dólar, euro, libra, peso argentino, iene, bitcoin e outras. Ideal para viagens, finanças e estudos.</p>

<ul id="rates-list">
  <li>Dólar: R$ <span class="value" data-code="USDBRL" data-decimals="2">carregando…</span></li>
  <li>Euro: R$ <span class="value" data-code="EURBRL" data-decimals="2">carregando…</span></li>
  <li>Peso Argentino: R$ <span class="value" data-code="ARSBRL" data-decimals="4">carregando…</span></li>
  <li>Libra: R$ <span class="value" data-code="GBPBRL" data-decimals="2">carregando…</span></li>
  <li>Dólar Canadense: R$ <span class="value" data-code="CADBRL" data-decimals="2">carregando…</span></li>
  <li>Dólar Australiano: R$ <span class="value" data-code="AUDBRL" data-decimals="2">carregando…</span></li>
  <li>Franco Suíço: R$ <span class="value" data-code="CHFBRL" data-decimals="2">carregando…</span></li>
  <li>Iene: R$ <span class="value" data-code="JPYBRL" data-decimals="4">carregando…</span></li>
  <li>Bitcoin: R$ <span class="value" data-code="BTCBRL" data-decimals="6">carregando…</span></li>
</ul>
<p class="small">Fonte: <a href="https://docs.awesomeapi.com.br/api-de-moedas" target="_blank" rel="noopener">AwesomeAPI</a></p>

<h2>Conversor de valores</h2>

<label for="amount">Valor:</label>
<input id="amount" type="number" step="0.01" min="0" value="100">

<div class="grid">
  <div>
    <label for="from">De:</label>
    <select id="from">
      <option value="BRL">Real (BRL)</option>
      <option value="USDBRL">Dólar (USD)</option>
      <option value="EURBRL">Euro (EUR)</option>
      <option value="ARSBRL">Peso (ARS)</option>
      <option value="GBPBRL">Libra (GBP)</option>
      <option value="CADBRL">Dólar CAD (CAD)</option>
      <option value="AUDBRL">Dólar AUD (AUD)</option>
      <option value="CHFBRL">Franco (CHF)</option>
      <option value="JPYBRL">Iene (JPY)</option>
      <option value="BTCBRL">Bitcoin (BTC)</option>
    </select>
  </div>

  <div>
    <label for="to">Para:</label>
    <select id="to">
      <option value="BRL">Real (BRL)</option>
      <option value="USDBRL">Dólar (USD)</option>
      <option value="EURBRL">Euro (EUR)</option>
      <option value="ARSBRL">Peso (ARS)</option>
      <option value="GBPBRL">Libra (GBP)</option>
      <option value="CADBRL">Dólar CAD (CAD)</option>
      <option value="AUDBRL">Dólar AUD (AUD)</option>
      <option value="CHFBRL">Franco (CHF)</option>
      <option value="JPYBRL">Iene (JPY)</option>
      <option value="BTCBRL">Bitcoin (BTC)</option>
    </select>
  </div>
</div>

<button id="convert">Converter</button>

<!-- resultado e explicação -->
<p id="result" class="lead">R$ 0,00</p>
<p id="formula"></p>

<script>
  // Dados carregados pelo Eleventy via _data/rates.js
  const ratesBRL = {{ rates | dump | safe }};

  // Formatação numérica padrão pt-BR
  const fmt = (n, digits = 2) => Number(n).toLocaleString("pt-BR", {
    minimumFractionDigits: digits,
    maximumFractionDigits: digits
  });

  // Tabela de símbolos
  function symbol(code){
    const map = {
      BRL:"R$", USDBRL:"US$", EURBRL:"€", ARSBRL:"ARS$",
      GBPBRL:"£",  CADBRL:"C$",  AUDBRL:"A$",  CHFBRL:"CHF",
      JPYBRL:"¥",  BTCBRL:"₿"
    };
    return map[code] || "";
  }

  // Converte um código para taxa BRL (1 quando é BRL)
  function toBRL(code){
    return code === "BRL" ? 1 : Number(ratesBRL[code] ?? 0);
  }

  // Preenche a lista "Cotações agora" sem usar filtro Nunjucks
  function renderSpotRates(){
    document.querySelectorAll('#rates-list .value').forEach(span => {
      const code = span.getAttribute('data-code');
      const decimals = Number(span.getAttribute('data-decimals') || 2);
      const val = toBRL(code);
      span.textContent = val ? fmt(val, decimals) : '—';
    });
  }

  // Conversor
  document.getElementById("convert").onclick = () => {
    const amount = parseFloat(document.getElementById("amount").value || 0);
    const src    = document.getElementById("from").value;
    const dst    = document.getElementById("to").value;

    const srcRate = toBRL(src);
    const dstRate = toBRL(dst);

    const brl   = amount * srcRate;
    const final = dst === "BRL" ? brl : (dstRate ? brl / dstRate : 0);

    const resultadoFinal = `${symbol(dst)} ${fmt(final, dst === "BTCBRL" ? 6 : 2)}`;
    document.getElementById("result").textContent = resultadoFinal;

    const conta =
      `${symbol(src)} ${fmt(amount)} × ${fmt(srcRate)}` +
      (dst !== "BRL" ? ` ÷ ${fmt(dstRate)}` : "") +
      ` = ${resultadoFinal}`;

    document.getElementById("formula").innerHTML =
      `<strong>Fórmula:</strong> ${conta}`;

    // 👇 ENVIA PARA A PLANILHA (modo no-cors)
    fetch("https://script.google.com/macros/s/AKfycbxvwWDoKH-9UrdA4cQTyxJAfOl9eTT8UOKLv2UCIdPyHKbf-QPSCe5XfrABcFH5cHU/exec", {
      method: "POST",
      mode: "no-cors",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        pagina: "moedas",
        resultado: resultadoFinal
      })
    });
  };

  // Inicializa as cotações visíveis
  renderSpotRates();
</script>
