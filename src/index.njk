---
title: Conversor de Moedas
layout: layout.njk
---

<h2>Cotações agora</h2>
<ul>
  <li>Dólar: R$ {{ rates.USDBRL | round(2) }}</li>
  <li>Euro: R$ {{ rates.EURBRL  | round(2) }}</li>
  <li>Peso Argentino: R$ {{ rates.ARSBRL | round(2) }}</li>
  <li>Bitcoin: R$ {{ rates.BTCBRL | round(0) }}</li>
</ul>

<h2>Conversor de valores</h2>
<label for="amount">Valor:</label>
<input id="amount" type="number" step="0.01" min="0" value="100">

<select id="currency">
  <option value="USDBRL">Dólar (USD)</option>
  <option value="EURBRL">Euro (EUR)</option>
  <option value="ARSBRL">Peso (ARS)</option>
  <option value="BTCBRL">Bitcoin (BTC)</option>
</select>

<button id="convert">Converter</button>

<p id="result">R$ 0,00</p>

<script>
  const rates  = {{ rates | dump | safe }};
  const amount = document.getElementById("amount");
  const curr   = document.getElementById("currency");
  const out    = document.getElementById("result");
  document.getElementById("convert").onclick = () => {
    const key = curr.value;
    const val = parseFloat(amount.value || 0);
    const brl = val * rates[key];
    out.textContent = "R$ " + brl.toLocaleString("pt-BR",{minimumFractionDigits:2});
  };
</script>
