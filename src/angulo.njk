---
title: Conversor de Ângulo
layout: layout.njk
---

<h1 class="text-3xl sm:text-4xl font-extrabold text-center text-gray-900">Conversor de Ângulo</h1>
<p class="text-center text-gray-600 mt-1">Graus, radianos, gradians (gon) e voltas.</p>

<section class="mt-8">
  <label for="angAmount" class="block text-sm mb-1">Valor:</label>
  <input id="angAmount" type="number" step="0.0001" value="180" class="w-full p-3 border border-gray-300 rounded-lg bg-white"/>

  <div class="grid mt-3">
    <div>
      <label for="angFrom" class="block text-sm mb-1">De:</label>
      <select id="angFrom" class="w-full p-3 border border-gray-300 rounded-lg bg-white">
        <option value="deg">Grau (°)</option>
        <option value="rad">Radiano (rad)</option>
        <option value="gon">Gradian (gon)</option>
        <option value="turn">Volta (turn)</option>
      </select>
    </div>
    <div>
      <label for="angTo" class="block text-sm mb-1">Para:</label>
      <select id="angTo" class="w-full p-3 border border-gray-300 rounded-lg bg-white">
        <option value="deg">Grau (°)</option>
        <option value="rad">Radiano (rad)</option>
        <option value="gon">Gradian (gon)</option>
        <option value="turn">Volta (turn)</option>
      </select>
    </div>
  </div>

  <button id="angConvert" type="button" class="btn w-full mt-3">Converter</button>

  <p id="angResult" class="bigResult">—</p>
  <p id="angFormula"></p>
</section>

<script>
  const angFmt=(n)=>Number(n).toLocaleString("pt-BR",{maximumFractionDigits:6});
  // fatores para radiano
  const PI=Math.PI;
  const AGL = { rad:1, deg:PI/180, gon:PI/200, turn:2*PI };

  angConvert.addEventListener("click", ()=>{
    const val = parseFloat(angAmount.value||0);
    const rad = val * AGL[angFrom.value];
    const out = rad / AGL[angTo.value];
    angResult.textContent = `${angFmt(out)} ${angTo.value}`;
    angFormula.innerHTML  = `<strong>Fórmula:</strong> via radianos (padrão)`;
  });
</script>
