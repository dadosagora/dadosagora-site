<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>{{ title or "ConvertePraMim" }}</title>

  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;700&display=swap" rel="stylesheet">
  <script src="https://cdn.tailwindcss.com"></script>

  {# CSS global publicado via passthrough: src/assets -> /assets #}
  <link rel="stylesheet" href="{{ '/assets/styles.css' | url }}">

  {# Fallback inline: garante estilo do botão/inputs mesmo se o arquivo externo não carregar #}
  <style>
    .btn{display:inline-block;width:100%;padding:.75rem 1rem;background:#2563eb;color:#fff;
         font-weight:600;text-align:center;border-radius:.5rem;border:0;cursor:pointer;
         transition:background-color .15s}
    .btn:hover{background:#1d4ed8}
    input[type="number"],input[type="text"],select{
      width:100%;padding:.75rem;border:1px solid #d1d5db;border-radius:.5rem;background:#fff
    }
    /* resultado grande */
    #result{margin-top:1.5rem;font-size:3rem;font-weight:700;color:#1d4ed8;text-align:center}
    /* box da fórmula */
    #formula{margin-top:1rem;font-size:.95rem;color:#555;background:#f4f4f4;padding:1rem;
             border-left:4px solid #1d4ed8;border-radius:4px}
  </style>
</head>
<body class="bg-gray-50 font-[Inter]">

  <!-- Cabeçalho -->
  <header class="bg-white shadow">
    <div class="max-w-6xl mx-auto px-4 py-4 flex flex-col items-center space-y-2 sm:space-y-0 sm:flex-row sm:justify-between">
      <a href="{{ '/' | url }}" class="text-2xl font-bold text-blue-600 text-center sm:text-left w-full sm:w-auto">
        ConvertePraMim
      </a>
      <nav class="space-x-4 text-sm text-gray-700 text-center sm:text-right w-full sm:w-auto">
        <a href="{{ '/' | url }}"             class="hover:text-blue-600">Início (Moedas)</a>
        <a href="{{ '/temperatura/' | url }}" class="hover:text-blue-600">Temperatura</a>
        <a href="{{ '/distancia/'   | url }}" class="hover:text-blue-600">Distância</a>
        <a href="{{ '/peso/'        | url }}" class="hover:text-blue-600">Peso</a>
        <a href="{{ '/volume/'      | url }}" class="hover:text-blue-600">Volume</a>
        <a href="{{ '/tempo/'       | url }}" class="hover:text-blue-600">Tempo</a>
      </nav>
    </div>
  </header>

  <!-- Conteúdo principal (injetado por cada página) -->
  <main class="max-w-xl mx-auto mt-10 p-6 bg-white rounded-xl shadow-md space-y-6">
    {{ content | safe }}
  </main>

  <!-- Rodapé -->
  <footer class="text-center text-sm text-gray-500 mt-16 pb-6">
    <p class="mb-2">© {{ site.year }} ConvertePraMim. Todos os direitos reservados.</p>
    <div class="space-x-4">
      <a href="{{ '/sobre/'       | url }}" class="text-blue-500 hover:underline">Sobre</a>
      <a href="{{ '/contato/'     | url }}" class="text-blue-500 hover:underline">Contato</a>
      <a href="{{ '/privacidade/' | url }}" class="text-blue-500 hover:underline">Privacidade</a>
    </div>
  </footer>

</body>
</html>
