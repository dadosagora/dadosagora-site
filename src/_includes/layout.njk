<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>{{ title or "ConvertePraMim" }}</title>

  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;700&display=swap" rel="stylesheet">
  <script src="https://cdn.tailwindcss.com"></script>

  <!-- CSS global publicado via passthrough: src/assets -> /assets -->
  <link rel="stylesheet" href="{{ '/assets/styles.css' | url }}">

  <!-- Fallback inline: garante estilo básico -->
  <style>
    /* evita scroll horizontal da página */
    body{ overflow-x:hidden; }

    .btn{display:inline-block;width:100%;padding:.75rem 1rem;background:#2563eb;color:#fff;
         font-weight:600;text-align:center;border-radius:.5rem;border:0;cursor:pointer;
         transition:background-color .15s}
    .btn:hover{background:#1d4ed8}
    input[type="number"],input[type="text"],select{
      width:100%;padding:.75rem;border:1px solid #d1d5db;border-radius:.5rem;background:#fff
    }
    #result{margin-top:1.5rem;font-size:3rem;font-weight:700;color:#1d4ed8;text-align:center}
    #formula{margin-top:1rem;font-size:.95rem;color:#555;background:#f4f4f4;padding:1rem;
             border-left:4px solid #1d4ed8;border-radius:4px}

    /* Nav scroll mobile */
    .nav-scroll{
      overflow-x:auto;
      -webkit-overflow-scrolling:touch;
      scrollbar-width:none;
      min-width:0;          /* <— crucial em layout flex */
      max-width:100%;
    }
    .nav-scroll::-webkit-scrollbar{display:none;}
    .nav-links{display:inline-flex;gap:.5rem;white-space:nowrap;}
    .nav-links a{display:inline-block;font-size:.95rem;padding:.5rem .75rem;border-radius:9999px;
      background:#f1f5f9;color:#334155;text-decoration:none;
      transition:background-color .15s ease,color .15s ease,transform .05s ease;}
    .nav-links a:hover{background:#e2e8f0;color:#1d4ed8;}
    .nav-links a:active{transform:scale(.98);}
    @media(min-width:1024px){
      .nav-scroll{overflow:visible;}
      .nav-links{gap:.75rem;flex-wrap:wrap;}
      .nav-links a{background:transparent;padding:.25rem .25rem;border-radius:.5rem;}
      .nav-links a:hover{background:#f1f5f9;}
    }
  </style>
</head>
<body class="bg-gray-50 font-[Inter]">

  <!-- Cabeçalho -->
  <header class="bg-white shadow sticky top-0 z-30">
    <div class="max-w-6xl mx-auto px-4 py-3 flex flex-col sm:flex-row sm:items-center sm:justify-between gap-2">
      <a href="{{ '/' | url }}" class="text-3xl sm:text-3xl font-extrabold text-blue-600">
        ConvertePraMim
      </a>

      <!-- Nav: ocupa 100% no mobile, fica ao lado no desktop -->
      <div class="nav-scroll w-full sm:w-auto">
        <nav class="nav-links" aria-label="Navegação principal">
          <a href="{{ '/' | url }}">Início (Moedas)</a>
          <a href="{{ '/temperatura/' | url }}">Temperatura e Sensação Térmica</a>
          <a href="{{ '/distancia/'   | url }}">Distância</a>
          <a href="{{ '/peso/'        | url }}">Peso</a>
          <a href="{{ '/volume/'      | url }}">Volume</a>
          <a href="{{ '/tempo/'       | url }}">Tempo</a>
          <a href="{{ '/area/'        | url }}">Área</a>
          <a href="{{ '/velocidade/'  | url }}">Velocidade</a>
          <a href="{{ '/energia/'     | url }}">Energia</a>
          <a href="{{ '/combustivel/' | url }}">Combustível</a>
          <a href="{{ '/culinaria/'   | url }}">Culinária</a>
          <a href="{{ '/roupas/'      | url }}">Roupas/Calçados</a>
          <a href="{{ '/dados/'       | url }}">Dados Digitais</a>
          <a href="{{ '/pressao/'     | url }}">Pressão</a>
          <a href="{{ '/angulo/'      | url }}">Ângulo</a>
        </nav>
      </div>
    </div>
  </header>

  <!-- Conteúdo principal -->
  <main class="max-w-xl mx-auto mt-10 p-6 bg-white rounded-xl shadow-md space-y-6">
    {{ content | safe }}
  </main>

  <!-- Rodapé -->
  <footer class="text-center text-sm text-gray-500 mt-16 pb-6">
    <p class="mb-2">© {{ site.year }} ConvertePraMim. Todos os direitos reservados.</p>
    <div class="space-x-4">
      <a href="{{ '/sobre/'       | url }}" class="text-blue-500 hover:underline">Sobre</a>
      <a href="{{ '/contato/'     | url }}" class="text-blue-500 hover:underline">Contato</a>
      <a href="{{ '/privacidade/' | url }}" class="text-blue-500 hover:underline">Privacidade</a>
    </div>
  </footer>

</body>
</html>
